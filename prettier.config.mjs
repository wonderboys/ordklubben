/**
 * Prettier owns code style in this repo.
 * ESLint focuses on correctness and framework-specific rules.
 */
const config = {
  singleQuote: true,
  printWidth: 100,
  semi: true,
  trailingComma: 'all',
  arrowParens: 'always',
  bracketSpacing: true,
};

export default config;
